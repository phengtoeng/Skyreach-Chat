<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light chat-app theme. Compose draws all UI; this styles the window/system bars. -->
    <style name="Theme.SplitSeal" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">#FF2E9BF6</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>
</resources>
